---
resources:
  - name: super-app
    type: git
    source:
      branch: ci
      uri: https://github.com/hellofresh/concourse-tutorial.git

jobs:
  - name: List
    plan:
    - get: super-app
      trigger: true
    - task: List
      config:
        platform: linux
        image_resource:
          type: docker-image
          source: {repository: ubuntu}
        run:
          path: ls
          args: ["-l"]

        inputs:
          - name: super-app
